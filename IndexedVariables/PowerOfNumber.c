#include <stdio.h>

int main()
{
    int base_of_the_number, power_of_the_number;

    printf("Enter the base of the number: ");
    scanf("%d",  &base_of_the_number);

    printf("Enter the power of the number: ");
    scanf("%d", &power_of_the_number);

    

    return 0;
}